/// animation_tail_apply(sprite, speed, x, y, angle, direction);
 // Script used to apply the tail parts for tails.

    if(argument0 != noone)
    {
       tails_tail_sprite     = argument0; 
       tails_tail_speed      = argument1; 
       tails_tail_x          = argument2;
       tails_tail_y          = argument3; 
       tails_tail_angle      = argument4; 
       tails_tail_direction  = argument5;  
    }
    else
    {
       tails_tail_sprite     = noone;
    }


