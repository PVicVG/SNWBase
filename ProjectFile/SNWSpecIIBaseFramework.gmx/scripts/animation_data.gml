/// animation_data()
 // Get the animation data of a instance.
 
 // Default Animations - Used for in case characters are just reskins or use the same values of
 // Sonic 3 sonic.
       animation_index_default();
   
 // Tails:
    if(player_index == CHAR_TAILS)
    {
       animation_index_tails();
    }
    
 // Knuckles:
    if(player_index == CHAR_KNUCKLES)
    {
       animation_index_knuckles();
    }    
    
 // Fang:
    if(player_index == CHAR_FANG)
    {
       animation_index_fang();
    }   
    
 // EggRobo:
    if(player_index == CHAR_EGGROBO)
    {
       animation_index_eggrobo();
    }   
    
 // Modern Sonic:
    if(player_index == CHAR_MODERN)
    {
       animation_index_modern();
    }   
    
 // Mighty:
    if(player_index == CHAR_MIGHTY)
    {
       animation_index_mighty();
    } 
    
 // Shadow:
    if(player_index == CHAR_SHADOW)
    {
       animation_index_shadow();
    } 
    
 // Amy:
    if(player_index == CHAR_AMY)
    {
       animation_index_amy();
    } 
    
 // Pingu:
    if(player_index == CHAR_PINGU)
    {
       animation_index_pingu();
    } 
